/**
 * @param {string} haystack
 * @param {string} needle
 * @return {number}
 */
var solve = function (haystack, needle) {
  console.log(haystack, needle);
  return haystack.indexOf(needle);
};

console.log(solve("veutn", "u"));
